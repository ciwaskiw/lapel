# Required for LLM features (scoring, profile build, tailor).
# `find --no-score`, `pipeline`, and `status` work without it.
ANTHROPIC_API_KEY=
# Optional model overrides
# LAPEL_MODEL_WORKER=claude-sonnet-4-6
# LAPEL_MODEL_SYNTH=claude-opus-4-8
# LAPEL_DEBUG=1
